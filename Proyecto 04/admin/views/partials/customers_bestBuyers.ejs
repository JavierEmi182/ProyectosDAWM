<main class="col-md-8 ms-sm-auto col-lg-10 px-md-4">
    <div class="table-responsive">
        <div class="table-wrapper">
            <div class="table-title">
                <div class="row">
                    <div class="col-sm-5">
                        <h2>Best Buyers</h2>
                    </div>
                </div>
            </div>
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Customer Number</th>
                        <th>Total Ammount</th>
                    </tr>
                </thead>
                <tbody>  
                    <% arrSales.forEach((sale, idx) => { %>  
                      <tr>
                        <td><%= sale._id%></td>  
                        <td id="ammount"><%= sale.totalAmmount%></td>  
                      </tr>  
                      <script>
                        function toFixed(num, fixed) {
                            var re = new RegExp('^-?\\d+(?:\.\\d{0,' + (fixed || -1) + '})?');
                            return num.toString().match(re)[0];
                        }

                        $(document).ready(function() {
                            var ammount='<%= sale.totalAmmount%>'
                            ammount=toFixed(parseFloat(ammount),2)
                            document.getElementById('ammount').innerText=ammount
                            });

                        document.addEventListener("DOMContentLoaded",function(event){
                            
                            var ammount='<%= sale.totalAmmount%>'
                            console.log("antes")
                            console.log(ammount)
                            ammount=toFixed(parseFloat(ammount),2)
                            ammount=toFixed(ammount,2)
                            console.log(ammount)
                            //document.getElementById('ammount').innerText=ammount
                            //document.getElementById('ammount').innerHTML=(ammount);

                        });

                      </script>
                    <% }); %>  
                </tbody>  
              </table>
        </div>
    </div>  
</main>